pipeline 
{
    agent any
    parameters 
    {
        string(name: 'WORKINGDIR', defaultValue: 'work/', description: 'download directory for json files')
        string(name: 'SERVERURL',  defaultValue: 'ftp5.gwdg.de', description: 'url of the https server with download packages')
        string(name: 'SERVERPATH', defaultValue: '/pub/misc/openstreetmap/openseamap/charts/history/kap', description: 'server path')
        string(name: 'FILENAME',   defaultValue: './sample/download.geojson', description: 'filename of merged geojson file')
        booleanParam(name: 'DEPLOY',     defaultValue: true, description: 'deploy files if true')
    }
    
    stages 
    {
	    
	    stage('fetch geojson files') {
	    	steps{
		        sh"""#!/bin/bash
		            set -x
		            cd OSM_pmd4dl
		            python3 FetchJsonFiles.py -a ${params.SERVERURL} -i ${params.SERVERPATH} -o ${params.WORKINGDIR}
		            rm -f ${params.WORKINGDIR}/download.geojson
		        """            
	        }
	    }
	    
	    stage('merge geojson files') {
	    	steps{
		        sh"""#!/bin/bash
		        set -x
		        cd OSM_pmd4dl
		        python3 merge_geojson_files.py -d ${params.WORKINGDIR} -o ${params.FILENAME} 
		        """            
		    }
	    }
	
	    stage('deploy geojson files') 
	    {
			when { 
				expression { return params.DEPLOY } 
			}
			
	        steps
            {
                sh"""#!/bin/bash
		        set -x
		        cd OSM_pmd4dl
		        echo rsync -e "ssh -i /var/lib/jenkins/sshkeys/id_osm" -av ${params.FILENAME} osm-trans@golf.franken.de:charts/kap/
		        echo rsync -e "ssh -i /var/lib/jenkins/sshkeys/osm-trans-alpha.pkey" -av ${params.FILENAME} osm-trans@alpha.openseamap.org:./
		    	ls sample/download.geojson
	        	"""  
		   }      
	     }     	
    }      
}